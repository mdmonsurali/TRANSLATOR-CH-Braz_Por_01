# Helper script dependencies on top of the vllm/vllm-openai base image
# (transformers, accelerate, huggingface_hub already provided by the base;
# datalab-to/chandra-ocr-2 weights are pre-baked at image build time).

# FastAPI app
fastapi==0.136.3
uvicorn[standard]==0.37.0
python-multipart==0.0.20

# vLLM client
openai==2.1.0

# PaddleOCR detection service client (diagram_text_recovery). Already present
# transitively via `openai`; pinned explicitly because we now import it directly.
httpx==0.28.1

# Document & image processing
pillow==11.3.0
PyMuPDF==1.27.2.3
python-docx==1.2.0

# Chandra layout-HTML parsing
beautifulsoup4==4.14.0

# Formula rendering
matplotlib==3.10.9

# Storage layer
asyncpg==0.31.0
minio==7.2.20
